import { KeyShortcutDirective } from "./key-shortcut/key-shortcut.directive";

describe("KeyShortcutDirective", () => {
  it("should create an instance", () => {
    const directive = new KeyShortcutDirective();
    expect(directive).toBeTruthy();
  });
});
